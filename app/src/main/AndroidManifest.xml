<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ganko.movil.ganko1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.menu.MenuActivity"
            android:theme="@style/NoActionBar">
        </activity>
        <activity android:name=".ui.farm.FarmActivity" />
        <activity
            android:name=".ui.login.password.PasswordActivity"
            android:theme="@style/NoActionBar" />
        <activity android:name=".ui.farm.add.AddFarmActivity" />
        <activity android:name=".ui.detail.DetailBovineActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.bovines.out.OutBovineActivity" />
        <activity android:name=".ui.bovines.add.AddBovineActivity" />
        <activity android:name=".ui.detail.reproductive.ReproductiveActivity" />
        <activity android:name=".ui.bovines.search.SearchBovineActivity"></activity>
    </application>

</manifest>